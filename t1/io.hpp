#pragma once

#include <iostream>
#include <vector>
#include "graph.hpp"
using namespace std;

void clearConsole();
int handleChoice(int minValue, int maxValue);
int requestMenuChoice(vector<string> headers);

